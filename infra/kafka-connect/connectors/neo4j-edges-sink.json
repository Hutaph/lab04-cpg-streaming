{
  "name": "neo4j-edges-sink",
  "config": {
    "topics": "cpg.edges",
    "connector.class": "streams.kafka.connect.sink.Neo4jSinkConnector",
    "errors.deadletterqueue.topic.name": "connector.errors",
    "errors.deadletterqueue.topic.replication.factor": "1",
    "errors.deadletterqueue.context.headers.enable": "true",
    "errors.tolerance": "all",
    "kafka.bootstrap.servers": "kafka:29092",
    "neo4j.server.uri": "bolt://cpg-neo4j:7687",
    "neo4j.authentication.basic.username": "neo4j",
    "neo4j.authentication.basic.password": "${NEO4J_PASSWORD}",
    "neo4j.encryption.enabled": "false",
    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "false",
    "neo4j.batch.size": "5",
    "neo4j.batch.timeout.msecs": "250",
    "neo4j.connection.acquisition.timeout.msecs": "10000",
    "neo4j.connection.liveness.check.timeout.msecs": "5000",
    "neo4j.topic.cypher.cpg.edges": "WITH event OPTIONAL MATCH (s_exist)-[r_exist:CPG_EDGE {edge_id: event.edge.edge_id}]->(d_exist) WITH event, s_exist, d_exist, r_exist, (CASE WHEN r_exist IS NOT NULL AND (s_exist.id <> event.edge.source_id OR d_exist.id <> event.edge.target_id) THEN true ELSE false END) AS mismatch WITH event, mismatch, (1 / (CASE WHEN mismatch THEN 0 ELSE 1 END)) AS ignore_me, r_exist WHERE NOT mismatch OPTIONAL MATCH (t_src:CPGNodeTombstone {id: event.edge.source_id, generation_id: event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version}) OPTIONAL MATCH (t_dst:CPGNodeTombstone {id: event.edge.target_id, generation_id: event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version}) OPTIONAL MATCH (te:CPGEdgeTombstone {id: event.edge.edge_id, generation_id: event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version}) WITH event, t_src, t_dst, te, r_exist WHERE t_src IS NULL AND t_dst IS NULL FOREACH (x IN CASE WHEN event.event_type = 'EDGE_UPSERT' AND te IS NULL THEN [1] ELSE [] END | MERGE (src:CPGNode {id: event.edge.source_id}) ON CREATE SET src.placeholder = true, src.file_id = event.file_id, src.generation_id = event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version, src.content_hash = event.content_hash, src.parser_version = event.parser_version, src.schema_version = event.schema_version, src.repository_id = event.repository_id MERGE (dst:CPGNode {id: event.edge.target_id}) ON CREATE SET dst.placeholder = true, dst.file_id = event.file_id, dst.generation_id = event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version, dst.content_hash = event.content_hash, dst.parser_version = event.parser_version, dst.schema_version = event.schema_version, dst.repository_id = event.repository_id MERGE (src)-[r:CPG_EDGE {edge_id: event.edge.edge_id}]->(dst) ON CREATE SET r += coalesce(event.edge.properties, {}), r.edge_type = event.edge.edge_type, r.repository_id = event.repository_id, r.file_id = event.file_id, r.parser_version = event.parser_version, r.schema_version = event.schema_version, r.content_hash = event.content_hash, r.generation_id = event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version ON MATCH SET r += coalesce(event.edge.properties, {}), r.edge_type = event.edge.edge_type, r.repository_id = event.repository_id, r.file_id = event.file_id, r.parser_version = event.parser_version, r.schema_version = event.schema_version, r.content_hash = event.content_hash, r.generation_id = event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version) FOREACH (x IN CASE WHEN event.event_type = 'EDGE_DELETE' THEN [1] ELSE [] END | MERGE (et:CPGEdgeTombstone {id: event.edge.edge_id, generation_id: event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version}) ON CREATE SET et.file_id = event.file_id, et.content_hash = event.content_hash, et.parser_version = event.parser_version, et.schema_version = event.schema_version) FOREACH (x IN CASE WHEN event.event_type = 'EDGE_DELETE' AND r_exist IS NOT NULL AND r_exist.generation_id = event.file_id + ':' + event.content_hash + ':' + event.parser_version + ':' + event.schema_version THEN [1] ELSE [] END | DELETE r_exist)"
  }
}
